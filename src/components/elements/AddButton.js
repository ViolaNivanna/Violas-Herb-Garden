import classes from "./AddButton.module.css";

const AddButton = () => {
    return (
        <button className={classes.addBtn}>
            <div>+ Adda badda</div>
        </button>
    );
};

export default AddButton;